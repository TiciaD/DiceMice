interface CountySelectionProps {
  onSelect: (countyId: string) => void
}

const CountySelection = ({ onSelect }: CountySelectionProps) => {
  return (
    <div>CountySelection</div>
  )
}

export default CountySelection